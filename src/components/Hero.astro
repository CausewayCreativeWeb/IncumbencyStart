<section id="top" class="hero">
  <div class="wrap">
    <p class="eyebrow">Astro one-pager starter</p>
    <h1>Ship your landing page in an afternoon.</h1>
    <p class="lead">
      A batteries-included Astro starter with a contact form powered by Resend, a
      newsletter signup wired to Mailchimp, and analytics tracking ready for Vercel.
    </p>
    <div class="cta">
      <a class="button primary" href="#newsletter">Join the newsletter</a>
      <a class="button ghost" href="#contact">Get in touch</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: 6rem;
    padding-bottom: 6rem;
    text-align: center;
  }

  .eyebrow {
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.8rem;
    font-weight: 600;
  }

  h1 {
    font-size: clamp(2.25rem, 5vw, 3.5rem);
    max-width: 42rem;
    margin: 0.5rem auto 1rem;
  }

  .lead {
    max-width: 36rem;
    margin: 0 auto 2rem;
    font-size: 1.05rem;
  }

  .cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .button {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
    transition: background-color 0.15s ease, border-color 0.15s ease;
  }

  .button.primary {
    background: var(--color-accent);
    color: white;
  }

  .button.primary:hover {
    background: var(--color-accent-hover);
  }

  .button.ghost {
    color: var(--color-text);
    border: 1px solid var(--color-border);
  }

  .button.ghost:hover {
    border-color: var(--color-accent);
  }
</style>
